{
  "name": "DetoxDemo",
  "version": "1.0",
  "devDependencies": {
    "detox": "^9.1.2",
    "jest": "^23.6.0"
  },
  "detox": {
    "test-runner": "jest",
    "specs": "e2e",
    "runner-config": "e2e/config.json",
    "configurations": {
      "ios.sim.release": {
        "binaryPath": "ios/build/Build/Products/Release-iphonesimulator/DetoxDemo.app",
        "build": "xcodebuild -workspace DetoxDemo.xcworkspace -scheme DetoxDemo -configuration Release -sdk iphonesimulator -derivedDataPath ios/build",
        "type": "ios.simulator",
        "name": "iPhone 7"
      }
    },
    "test-runner": "jest"
  }
}